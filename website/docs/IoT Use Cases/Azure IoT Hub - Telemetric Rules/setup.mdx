---
sidebar_position: 1
---
# Setup

In this part you will setup your device with our software to run the onboarding process.

## Download

Start with downloading the software package in the appropriate version for your machine.

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs>
  <TabItem value="x86" label="x86" default>

Download package for **[x86 architecture](./assets/demo_test_device_1_x86.tar.gz)**.  
Alternatively you can use this curl command:  
```
curl -o devity_onboarding_package.tar.gz "https://path/to/download/package/demo_x86.tar.gz"
```
  </TabItem>
  <TabItem value="armv7" label="armv7">

Download package for **[armv7 architecture](./assets/demo_test_device_1_x86.tar.gz)**.  
Alternatively you can use this curl command:  
```
curl -o devity_onboarding_package.tar.gz "https://path/to/download/package/demo_armv7.tar.gz"
```
  </TabItem>
</Tabs>

## Unpack

Open a terminal and unpack the download package to your desired destination.
Make sure that your current user that read, write and execution permission in that folder.  
```
tar -xvf devity_onboarding_package.tar.gz
``` 
  
In the package you just unpacked are three sperate software packages.
The package should look like this.
```
.
├── cert_man
│   ├── authentication (with certificate management server)
│   ├── cert_man (executable)
│   └── config (configurations for )
├── conf_man
│   ├── conf.d
│   ├── conf_man
│   └── templates
├── mqtt_client
│   └── demo-client
├── onboarder
├── onboarding_identity
│   └── identity files
└── truststore

```

- onboarder and onboarding_identity
  - Recieves initial data and configurations
- Certificate management
  - Generates new certificates and renews them when expiring
- Configuration management
  - Generates and updates configurations for other apllications (e.g. certificate management)
- truststore
  - Stores certificate authority certificates

Now proceed to configurating your devie.
